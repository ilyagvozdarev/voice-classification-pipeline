# Deps for the vLLM backend. vLLM pulls in a CUDA build of torch itself and
# requires an NVIDIA GPU + driver at runtime.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
vllm==0.6.6
